spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:3306/centralized_tests?createDatabaseIfNotExist=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: FILTER-SERVICES
server:
  port: 8085